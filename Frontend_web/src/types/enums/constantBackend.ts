export enum Gender {
  Male = '1',
  Female = '2',
  Other = '3',
}

export enum SearchService {
  Product = 1,
  BlindBox = 2,
}

export enum SortBy {
  Sort = 0,
  MostPopular = 1,
  DecreasingPrice = 2,
  IncreasingPrice = 3,
  Newest = 4,
  Oldest = 5,
}
